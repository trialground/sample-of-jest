module.exports = function sum(a, b) {
    return a + b;
}

// To start up, we need:
// 1. yarn install --dev jest
// 2. Create a 'test' dir
// 3. Create some files named with a postfix '.test.js'
// 4. Write testing scripts
// 5. Add `"test": "jest"` into package.json in the 'scripts' field
